# ETL Pipeline — NYC Yellow Taxi

A modular Python-based ETL (Extract, Transform, Load) pipeline built around the NYC Yellow Taxi dataset. This project demonstrates the ability to ingest data from multiple source types, apply transformation logic, and load the results into a target database or file store.

Built as a data engineering portfolio project.

---

